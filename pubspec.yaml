name: flake
description: Snake, in Flutter.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  snake: ^0.1.0
  simple_gesture_detector: ^0.2.0

dev_dependencies:
  lint: ^1.5.3

flutter:
  uses-material-design: true
